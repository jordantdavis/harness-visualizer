{
  "name": "hv-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "postbuild": "node -e \"require('fs').closeSync(require('fs').openSync('../internal/web/dist/.gitkeep','a'))\"",
    "preview": "vite preview",
    "test": "vitest run"
  },
  "dependencies": {
    "lit": "^3.2.1"
  },
  "devDependencies": {
    "happy-dom": "^15.11.7",
    "typescript": "^5.7.2",
    "vite": "^6.0.5",
    "vitest": "^3.0.0"
  }
}
